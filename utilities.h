#ifndef UTILITIES
#define UTILITIES

int checkForNonStdDigits(char string[]);
int checkForLetters(char string[]);
int checkForNumbers(char string[]);
void clearTerminal();
int sleep(long msec);

#endif 